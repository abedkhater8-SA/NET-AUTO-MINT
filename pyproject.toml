[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "net-auto-mint"
version = "0.1.0"
description = "Network automation lab project"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["src*"]